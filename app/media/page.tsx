import { MediaContent } from "../../components/media-content";

export const metadata = {
  title: 'Street Fellowship - Faithybites',
  description: 'Photos from our street fellowship outreach.',
};

export default function MediaPage() {
  return <MediaContent />;
}